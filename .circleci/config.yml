version: 2.1

executors:
  machine-executor:
    machine:
      image: ubuntu-2204:current  

jobs:
  build-test-deploy:
    executor: machine-executor
    steps:
      - checkout:
          path: ~/project  

      - run:
          name: Install Dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y unzip
            curl -fsSL https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
            echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
            sudo apt-get update && sudo apt-get install terraform awscli

      - run:
          name: Extract DB Name from Branch
          command: |
            DB_NAME=$(echo $CIRCLE_BRANCH | sed 's/feature\///' | sed 's/create-//')
            echo "DB_NAME=$DB_NAME" >> $BASH_ENV

      - run:
          name: Deploy RDS via Terraform
          command: |
            cd ~/project/terraform  
            terraform init
            terraform apply -auto-approve \
            -var="db_name=$DB_NAME" \
            -var="secret_name=mysql/$DB_NAME/DB_CREDENTIALS"

workflows:
  version: 2
  deploy_on_merge:
    jobs:
      - build-test-deploy:
          filters:
            branches:
              only:
                - main
                - feature/*